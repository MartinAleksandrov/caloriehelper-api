# Production-specific overrides
# This file activates when SPRING_PROFILES_ACTIVE=prod

# Don't auto-modify schema in production
spring.jpa.hibernate.ddl-auto=validate

# Reduce log noise
spring.jpa.show-sql=false
logging.level.org.hibernate.SQL=WARN
logging.level.org.springframework.security=WARNg